// Funciones de tipo Expresión 

// Declaración Función de tipo Expresión 
let sumar = function (a, b){return a + b}; // Como no se le asigna un nombre a esta si termina en ;


// Llamamos a la función
resultado = sumar(1, 2);
console.log(resultado);